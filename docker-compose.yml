services:
  sslautomation:
    image: automationapp:latest
    container_name: sslautomation
    networks:
      - SSLAutomationNetwork
    ports:
      - "3000:3000"

networks:
  SSLAutomationNetwork: